#include <stdio.h>
#include "mtk_c.h"
#define MAX 1024

void main(){
  //hardware initialization
  init_kernel(); // maybe?
  
  //system setting
  set_task(task1);
  set_task(task2);
  begin_sch(); //start multitasking
}
